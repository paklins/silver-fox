import { SystemButtonDirective } from './system-button.directive';

describe('SystemButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SystemButtonDirective();
    expect(directive).toBeTruthy();
  });
});
