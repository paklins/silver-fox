import { ApplicationHeaderDirective } from './application-header.directive';

describe('ApplicationHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplicationHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
