import { CollapsibleSidebarDirective } from './collapsible-sidebar.directive';

describe('CollapsibleSidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new CollapsibleSidebarDirective();
    expect(directive).toBeTruthy();
  });
});
